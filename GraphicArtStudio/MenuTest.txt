菜单测试说明：

1. 编译并运行程序
2. 检查编辑菜单项是否现在可以点击（不再是灰色）
3. 点击"撤销"应该显示"Undo function called!"消息
4. 如果菜单项仍然是灰色，问题可能在于：
   - 菜单资源文件(.rc)中的ID不匹配
   - 消息映射表有问题
   - MFC框架没有正确连接

临时修改说明：
- 所有OnUpdate函数现在都返回TRUE（强制启用）
- OnEditUndo添加了调试消息
- 添加了Resource.h包含

如果测试成功，我们可以恢复原始的条件逻辑。
